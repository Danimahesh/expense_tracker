<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required only for `flutter run --profile` tooling. Not in the release APK. -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
